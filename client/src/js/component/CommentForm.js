var React=require('react');
//var ReactDOM=require('react-dom');

var CommentForm=React.createClass({

  render: function () {
      return(
        <div>

      <h1>you are in Comment Form</h1>
        </div>
      )
  }
})

module.exports=CommentForm;
